{
  "name": "ktet-quiz",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "vercel dev",
    "build": "tailwindcss -i ./public/input.css -o ./public/css/style.css --minify",
    "deploy": "npm run build && vercel --prod"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
